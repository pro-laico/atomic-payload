import type { APFieldWrapper } from '../types';
/**
 * Reusable Atomic Payload `active` checkbox field. Wires the `active` APF flag
 * and ships with sensible admin defaults.
 */
export declare const ActiveField: APFieldWrapper<'checkbox', 'name' | 'apf' | 'type' | 'defaultValue'>;
//# sourceMappingURL=active.d.ts.map