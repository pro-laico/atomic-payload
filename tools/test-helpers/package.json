{
  "name": "@tools/test-helpers",
  "version": "0.0.0",
  "description": "Internal test helpers — boots a real Payload on in-memory SQLite for integration tests. Never published; private workspace package.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@payloadcms/db-sqlite": "3.79.0",
    "mongodb-memory-server": "^10.1.4",
    "payload": "^3.79.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.15",
    "typescript": "5.8.3"
  }
}
