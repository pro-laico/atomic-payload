---
title: "@pro-laico/core"
description: "The shared foundation every Atomic Payload package builds on: end-to-end type safety, tag-based caching and revalidation, JSON-schema type generation, and reusable admin and frontend building blocks."
---

`@pro-laico/core` is the shared foundation the rest of Atomic Payload is built on. You rarely install it on its own (the other plugins bring it along), but it's the common ground they all rely on: end-to-end type safety, tag-based caching with correct revalidation, and a set of reusable fields, hooks, and UI pieces. This page is mostly a reference for what it provides.

## Installation

<Tabs groupId="package-manager" items={['pnpm', 'npm', 'yarn']}>

<Tab value="pnpm">

```bash
pnpm add @pro-laico/core
```

</Tab>

<Tab value="npm">

```bash
npm install @pro-laico/core
```

</Tab>

<Tab value="yarn">

```bash
yarn add @pro-laico/core
```

</Tab>

</Tabs>

<Callout type="info">
  **Installed for you:** this package's dependencies (including `server-only`).
  **Provide yourself (peers):** `payload`, `next`, `react`, and `@payloadcms/ui` come with a Payload + Next.js app; also install `@base-ui/react` (the UI primitives) and, if you use live preview, `@payloadcms/live-preview-react`.
</Callout>

## Setup

You usually get `@pro-laico/core` transitively, and the [atomic-payload template](/docs/templates/atomic-payload) wires up everything below for you. When you reach for it directly, these are the pieces worth knowing.

<Steps>

<Step>
### Revalidate caches with `revalidationPlugin`

Every `@pro-laico/*` collection and global bakes its own cache-revalidation hooks, so they need no wiring. `revalidationPlugin` covers the gap: it attaches the core revalidation hooks (post-commit `afterChange`, plus `afterDelete`) to the collection and global slugs you name, for collections a third-party plugin registers (for example the form builder's `forms` / `form-submissions`). Add it after the plugin that registers those slugs:

```ts
import { buildConfig } from 'payload'
import { revalidationPlugin } from '@pro-laico/core'
import { formBuilderPlugin } from '@payloadcms/plugin-form-builder'

export default buildConfig({
  plugins: [
    formBuilderPlugin({ /* ... */ }),
    // After the plugin that registers them, so the slugs already exist:
    revalidationPlugin({ collectionSlugs: ['forms', 'form-submissions'] }),
  ],
})
```

The dispatchers self-select by slug and no-op when no handler matches, so an unknown slug is harmless. The [atomic-payload template](/docs/templates/atomic-payload) wires its plugins as a flat `Plugin[]` and uses this for the form builder's collections.
</Step>

<Step>
### Generate accurate block types with `jsonSchemaPlugin`

`jsonSchemaPlugin` extends `payload generate:types` so block fields get accurate generated types. Add it to your `plugins` array, giving it the block lists each `@pro-laico` plugin contributes so the generated types stay precise. The template registers it this way.
</Step>

<Step>
### Generate types with the augmentation step

`@pro-laico/core` includes a `core-augment-types` command that writes a `payload-types.augment.d.ts` file next to your generated `payload-types.ts`. It connects your project's concrete generated types into the shared type system, so every plugin's schema stubs resolve to your real shapes instead of permissive fallbacks. Chain it after Payload's own type generation, exactly what the template's `generate:types` script does:

```json
{
  "scripts": {
    "generate:types": "payload generate:types && core-augment-types"
  }
}
```

The generated `payload-types.augment.d.ts` is rewritten every time you run this, so add it to your `.gitignore` next to the rest of the generated types:

```plaintext
# .gitignore — generated by payload generate:types
src/payload-types.ts
src/payload-types.augment.d.ts
```
</Step>

</Steps>

The rest of the package (fields, hooks, cache helpers, URL and metadata utilities, and the admin / frontend components) is imported directly where you need it. See the [Exports](#exports) table for the full surface and the import path for each item.

## Options

### `revalidationPlugin(options?)`

<TypeTable
  type={{
    enabled: { type: 'boolean', default: 'true', description: 'Set false to turn the plugin off (it becomes a no-op).' },
    collectionSlugs: { type: 'string[]', default: '[]', description: 'Collections that should revalidate cached content when a document changes (an afterChange hook).' },
    deleteCollectionSlugs: { type: 'string[]', default: '[]', description: 'Collections that should revalidate cached content when a document is deleted (an afterDelete hook).' },
    globalSlugs: { type: 'string[]', default: '[]', description: 'Globals that should revalidate cached content when they change (an afterChange hook).' },
  }}
/>

All options at their defaults, as a working starting point:

```ts
revalidationPlugin({
  enabled: true,
  collectionSlugs: [],
  deleteCollectionSlugs: [],
  globalSlugs: [],
})
```

### `jsonSchemaPlugin(options)`

<TypeTable
  type={{
    enabled: { type: 'boolean', default: 'true', description: 'Set false to turn the plugin off (it becomes a no-op).' },
    toJSONSchemaExtensions: { type: 'ToJSONSchemaExtensionsFn', description: 'Required. The base schema-extension function (the template uses the one from @pro-laico/zap).' },
    generateBlocksType: { type: 'GenerateBlocksTypeFn', description: 'Required. Builds a JSON-schema definition for a named set of block references (also from @pro-laico/zap).' },
    blocks: { type: 'AtomicPayloadSchemaBlocks', description: 'Required. The block reference lists each plugin contributes (child blocks, action blocks, form blocks, and so on) so their generated types are precise. See its keys below.' },
    extraDefinitions: { type: 'Record<string, JSONSchema4>', description: 'Optional per-project schema definitions, merged in last so they can override the defaults.' },
  }}
/>

<Accordions>

<Accordion title="Advanced: block lists">

The `blocks` option is an object of named block-reference lists (each a `(string | undefined)[]` of block slugs). Pass the `.options` array each plugin exports:

<TypeTable
  type={{
    ChildBlocks: { type: 'BlockRefs', description: 'Required. The block slugs allowed inside atomic child areas (from @pro-laico/atomic).' },
    BackdropChildren: { type: 'BlockRefs', description: 'The block slugs allowed inside a backdrop child area. Optional, skipped when not passed.' },
    ActionBlocks: { type: 'BlockRefs', description: 'Required. The action block slugs (from @pro-laico/atomic actions).' },
    FormRateLimitBlocks: { type: 'BlockRefs', description: 'Required. The form rate-limit block slugs.' },
    FormSanitationBlocks: { type: 'BlockRefs', description: 'Required. The form sanitation block slugs.' },
    FormValidationBlocks: { type: 'BlockRefs', description: 'Required. The form validation block slugs.' },
    InputSanitationBlocks: { type: 'BlockRefs', description: 'Required. The per-input sanitation block slugs.' },
    InputValidationBlocks: { type: 'BlockRefs', description: 'Required. The per-input validation block slugs.' },
  }}
/>

</Accordion>

</Accordions>

All options at their defaults, with the block lists wired the way the template does it:

```ts
jsonSchemaPlugin({
  enabled: true,
  toJSONSchemaExtensions, // required (from @pro-laico/zap)
  generateBlocksType, // required (from @pro-laico/zap)
  blocks: {
    ChildBlocks: ChildBlockType.options, // required
    BackdropChildren: BackdropChildSlug.options, // optional
    ActionBlocks: ActionBlockType.options, // required
    FormRateLimitBlocks: FormRateLimitBlockType.options, // required
    FormSanitationBlocks: FormSanitationBlockType.options, // required
    FormValidationBlocks: FormValidationBlockType.options, // required
    InputSanitationBlocks: InputSanitationBlockType.options, // required
    InputValidationBlocks: InputValidationBlockType.options, // required
  },
  // extraDefinitions is unset by default
})
```

## Environment variables

`@pro-laico/core` reads these at runtime. Most have sensible fallbacks; set the ones the features you use depend on.

| Variable | Required? | Purpose |
| --- | --- | --- |
| `NEXT_PUBLIC_SERVER_URL` | No | The site's base URL, used by `getServerSideURL` for absolute links and metadata. Falls back to the Vercel production URL, then `http://localhost:3000`. |
| `VERCEL_PROJECT_PRODUCTION_URL` | No | Set by Vercel. Used as the base URL when `NEXT_PUBLIC_SERVER_URL` isn't set. |
| `PREVIEW_SECRET` | For previews | Validates draft-preview requests: `generateLivePreviewPath` appends it to the preview URL and the preview route handler rejects mismatches. |
| `VERCEL_DEPLOY_WEBHOOK_URL` | For the deploy control | The deploy hook the admin "trigger deploy" control (the site triggers component) posts to. |
| `LOGS` | No | Set to `true` to print the package's cache, revalidation, and preview debug logs. |

## Caching & revalidation

`@pro-laico/core` ships the caching machinery the rest of Atomic Payload reads through, but no data getters of its own. The pieces it owns:

- `withCache` and `mt` (from `@pro-laico/core/cache`): the primitive every package getter wraps its fetch with, plus the merge-tags helper that builds the dependency-tag strings. Server-only.
- The revalidation hooks and factories (`revalidateCacheCollection`, `revalidateCacheCollectionAfterChange`, `revalidateCacheOnDelete`, `revalidateCacheGlobalAfterChange`, and the `createRevalidateCache*` factories): `revalidationPlugin` attaches them to the slugs you name so saving or deleting a document revalidates the matching tags.
- `revalidateTag`: the safe `next/cache` wrapper used in server actions and the admin deploy control.

See [Caching & revalidation](/docs/concepts/caching) for how the tags are derived and how a read-side tag matches the revalidate side.

## Exports

`@pro-laico/core` is large and spans several import subpaths, so its surface is grouped below by what each piece does and where it comes from.

### Plugins & composition

<ExportTable
  exports={{
    revalidationPlugin: {
      type: 'plugin',
      location: '@pro-laico/core',
      description: 'Attaches the cache-revalidation hooks (afterChange + afterDelete) to the collection and global slugs you list. The @pro-laico/* collections and globals bake these hooks themselves, so use this for third-party collections (e.g. the form builder\'s forms / form-submissions). Default and named export.',
      params: {
        'options?': { type: 'RevalidationPluginOptions', description: 'See the Options table above (`collectionSlugs`, `deleteCollectionSlugs`, `globalSlugs`, `enabled`). Defaults to an empty config, which is a no-op.' },
      },
      returns: { type: 'Plugin', description: 'A Payload config plugin you add to `buildConfig({ plugins: [...] })`, after the plugin that registers the target slugs.' },
      example: `import { buildConfig } from 'payload'
import { revalidationPlugin } from '@pro-laico/core'
import { formBuilderPlugin } from '@payloadcms/plugin-form-builder'

export default buildConfig({
  plugins: [
    formBuilderPlugin({ /* ... */ }),
    revalidationPlugin({ collectionSlugs: ['forms', 'form-submissions'] }),
  ],
})`,
    },
    jsonSchemaPlugin: {
      type: 'plugin',
      location: '@pro-laico/core',
      description: 'Extends payload generate:types with accurate JSON-schema definitions for block fields. Default and named export.',
      params: {
        options: { type: 'JSONSchemaPluginOptions', description: 'See the Options table above. `toJSONSchemaExtensions`, `generateBlocksType`, and `blocks` are required; pass each plugin\'s block-slug `.options` arrays.' },
      },
      returns: { type: 'Plugin', description: 'A Payload config plugin that appends the schema extension to `config.typescript.schema`.' },
      example: `import { jsonSchemaPlugin } from '@pro-laico/core'
import { generateBlocksType, toJSONSchemaExtensions } from '@pro-laico/zap'
import { ChildBlockType } from '@pro-laico/atomic/children/zap'
import { ActionBlockType } from '@pro-laico/atomic/actions/zap'

export const jsonSchemaPluginConfig = jsonSchemaPlugin({
  toJSONSchemaExtensions,
  generateBlocksType,
  blocks: {
    ChildBlocks: ChildBlockType.options,
    ActionBlocks: ActionBlockType.options,
    // ...the form / input block lists
  },
})`,
    },
    createJSONSchemaExtensions: {
      type: 'function',
      location: '@pro-laico/core',
      description: 'Builds the schema-extension function jsonSchemaPlugin appends. Call it directly to wire schema generation onto config.typescript.schema by hand instead of through the plugin.',
      params: {
        options: { type: 'CreateJSONSchemaExtensionsOptions', description: 'Same `toJSONSchemaExtensions`, `generateBlocksType`, `blocks`, and optional `extraDefinitions` the plugin takes (minus `enabled`).' },
      },
      returns: { type: '(args: { jsonSchema }) => JSONSchema4', description: 'A schema-extension function you push onto `config.typescript.schema`.' },
      example: `import { buildConfig } from 'payload'
import { createJSONSchemaExtensions } from '@pro-laico/core'
import { generateBlocksType, toJSONSchemaExtensions } from '@pro-laico/zap'

const schemaExtension = createJSONSchemaExtensions({
  toJSONSchemaExtensions,
  generateBlocksType,
  blocks: { /* ...the block-slug lists... */ },
})

export default buildConfig({
  typescript: { schema: [schemaExtension] },
})`,
    },
  }}
/>

### Fields

<ExportTable
  exports={{
    slugField: {
      type: 'field',
      location: '@pro-laico/core',
      description: 'Factory for a paired slug + slugLock field set with the auto-formatting hook attached. Returns both fields as a tuple you spread into a collection.',
      params: {
        slugPath: { type: 'string', description: 'The admin component path your project provides for the custom Slug UI (e.g. SlugPath, or your own).' },
        'fieldToUse?': { type: 'string', description: 'The field the slug is formatted from. Defaults to `title`.' },
        'overrides?': { type: '{ slugOverrides?, checkboxOverrides? }', description: 'Partial field overrides merged onto the slug and slugLock fields.' },
      },
      returns: { type: '[TextField, CheckboxField]', description: 'The slug field and its hidden slugLock checkbox.' },
      example: `import type { CollectionConfig } from 'payload'
import { slugField, SlugPath } from '@pro-laico/core'

export const Pages: CollectionConfig = {
  slug: 'pages',
  fields: [
    { name: 'title', type: 'text' },
    ...slugField(SlugPath, 'title'),
  ],
}`,
    },
    StorageTab: {
      type: 'field',
      location: '@pro-laico/core',
      description: 'Factory for the admin Storage tab holding read-only JSON fields for the generated atomic classes, forms, and actions.',
      params: {
        'args?': { type: "{ filter?: ('classes' | 'forms' | 'actions')[] }", description: 'Limit which storage groups the tab renders. Defaults to all three.' },
      },
      returns: { type: 'TabAsField', description: 'A tab field you add to a collection or global `fields` array.' },
      example: `import { StorageTab } from '@pro-laico/core'

export const Pages = {
  slug: 'pages',
  fields: [
    { type: 'tabs', tabs: [/* your content tabs */] },
    StorageTab(),
  ],
}`,
    },
    DevModeField: {
      type: 'field',
      location: '@pro-laico/core',
      description: 'Factory for a checkbox that surfaces developer-only controls in the admin. Takes no arguments.',
      returns: { type: 'CheckboxField', description: 'A `devMode` checkbox field.' },
      example: `import { DevModeField } from '@pro-laico/core'

const fields = [DevModeField()]`,
    },
    TestPathField: {
      type: 'field',
      location: '@pro-laico/core',
      description: 'A relationship field for the live-preview test path, bound to the `pages` collection. Equivalent to `createTestPathField()`; use `createTestPathField(slug)` for a non-default pages slug.',
    },
    createTestPathField: {
      type: 'function',
      location: '@pro-laico/core',
      description: 'Factory for the live-preview `testPath` relationship field, bound to the pages collection slug you pass.',
      params: {
        'pagesSlug?': { type: 'string', description: 'The pages collection slug the relationship points at. Defaults to `pages`.' },
      },
      returns: { type: 'RelationshipField', description: 'A `testPath` relationship field for that pages slug.' },
      example: `import { createTestPathField } from '@pro-laico/core'

const fields = [createTestPathField('docs')]`,
    },
    UniqueTitleField: {
      type: 'field',
      location: '@pro-laico/core',
      description: 'Factory for a required, unique title text field.',
      params: {
        'defaultValue?': { type: 'string', description: 'The title default. Defaults to `New Title`.' },
      },
      returns: { type: 'TextField', description: 'A unique `title` text field.' },
      example: `import { UniqueTitleField } from '@pro-laico/core'

const fields = [UniqueTitleField('Untitled')]`,
    },
    ActiveField: {
      type: 'field',
      location: '@pro-laico/core',
      description: 'Factory for the reusable active / enabled checkbox, pre-wired with the `active` APF flag.',
      params: {
        'args?': { type: 'Partial<APArgs<\'checkbox\'>>', description: 'Field overrides (label, admin, defaultValue, …) merged onto the base active field.' },
      },
      returns: { type: 'CheckboxField', description: 'An `active` checkbox field.' },
      example: `import { ActiveField } from '@pro-laico/core'

const fields = [ActiveField({ defaultValue: false })]`,
    },
    APField: {
      type: 'function',
      location: '@pro-laico/core',
      description: 'Wraps a Payload field config to add Atomic Payload Functions (APF) behavior, so the field can drive configurable block behavior. The building block ActiveField and the other APF fields are built on.',
      params: {
        args: { type: "APArgs<'text' | 'select' | 'number' | 'textarea' | 'checkbox'>", description: 'A field config plus an `apf` array naming the functions it drives.' },
      },
      returns: { type: 'Field', description: 'The field config with the APF admin components and hooks wired in.' },
      example: `import { APField } from '@pro-laico/core'

const activeField = APField({
  type: 'checkbox',
  name: 'active',
  apf: ['active'],
})`,
    },
    generateAPFFields: {
      type: 'function',
      location: '@pro-laico/core',
      description: 'Builds the hidden, virtual APF storage checkbox fields for the named functions. Add the result to any collection whose documents run APF behavior.',
      params: {
        apFunctions: { type: 'APFunction[]', description: 'The function names to generate storage fields for (e.g. `active`, `form`, `page`).' },
      },
      returns: { type: 'CheckboxField[]', description: 'The virtual APF storage fields.' },
      example: `import { generateAPFFields } from '@pro-laico/core'

const fields = [...generateAPFFields(['active', 'page'])]`,
    },
    runAPF: {
      type: 'function',
      location: '@pro-laico/core',
      description: 'Reads whether an APF was marked changed for a document in the current request context. Call it from a hook to branch on whether a given function ran.',
      params: {
        args: { type: '{ context, id, apf }', description: 'The request `context`, the document `id`, and the `apf` function name to check.' },
      },
      returns: { type: 'boolean', description: 'True when the per-id/apf flag is set for the request.' },
      example: `import { runAPF } from '@pro-laico/core'

// inside an afterChange hook:
if (runAPF({ context, id: doc.id, apf: 'page' })) {
  // the page APF ran for this save
}`,
    },
  }}
/>

### Hooks

<ExportTable
  exports={{
    'revalidate hooks': {
      type: 'function',
      location: '@pro-laico/core',
      description: 'The ready-bound collection and global revalidation hooks (revalidateCacheCollection, revalidateCacheCollectionAfterChange, revalidateCacheOnDelete, revalidateCacheGlobalAfterChange), wired for you by revalidationPlugin. They dispatch to a default per-slug handler map that matches the template\'s collections.',
    },
    'createRevalidateCache* factories': {
      type: 'function',
      location: '@pro-laico/core',
      description: 'Bind your own slug-to-handler map: createRevalidateCache (beforeChange), createRevalidateCacheAfterChange (afterChange, preferred for plain collections), and createRevalidateCacheOnDelete (afterDelete). Each takes a `{ [slug]: handler }` record and returns the matching Payload hook.',
      params: {
        handlers: { type: 'Record<string, (ctx) => void | Promise<void>>', description: 'Maps a collection slug to the revalidation handler that runs for its writes.' },
      },
      returns: { type: 'CollectionBeforeChangeHook | CollectionAfterChangeHook | CollectionAfterDeleteHook', description: 'A hook that dispatches to the matching slug handler (and skips while seeding).' },
      example: `import type { CollectionConfig } from 'payload'
import { createRevalidateCacheAfterChange, revalidateTag } from '@pro-laico/core'

const revalidate = createRevalidateCacheAfterChange({
  articles: async ({ data }) => {
    await revalidateTag('article', data?.slug)
  },
})

export const Articles: CollectionConfig = {
  slug: 'articles',
  hooks: { afterChange: [revalidate] },
  fields: [/* ... */],
}`,
    },
    updateHrefHook: {
      type: 'function',
      location: '@pro-laico/core',
      description: 'A field hook that keeps a document `href` in sync with its latest breadcrumb as its slug or parents change. Attach it to an href field\'s `beforeChange`.',
      example: `import { updateHrefHook } from '@pro-laico/core'

const hrefField = {
  name: 'href',
  type: 'text',
  hooks: { beforeChange: [updateHrefHook] },
}`,
    },
    updatePublishedAtHook: {
      type: 'function',
      location: '@pro-laico/core',
      description: 'A field hook that stamps `publishedAt` the first time a document is published. Attach it to a date field\'s `beforeChange`.',
      example: `import { updatePublishedAtHook } from '@pro-laico/core'

const publishedAtField = {
  name: 'publishedAt',
  type: 'date',
  hooks: { beforeChange: [updatePublishedAtHook] },
}`,
    },
  }}
/>

### URL & metadata helpers

<ExportTable
  exports={{
    getServerSideURL: {
      type: 'function',
      location: '@pro-laico/core',
      description: 'Resolves the site base URL on the server. Reads NEXT_PUBLIC_SERVER_URL, then the Vercel production URL, then falls back to http://localhost:3000. Takes no arguments.',
      returns: { type: 'string', description: 'The absolute base URL for the current environment.' },
      example: `import { getServerSideURL } from '@pro-laico/core'

// in a server component or sitemap route:
const canonical = \`\${getServerSideURL()}/about\``,
    },
    GenerateMetaData: {
      type: 'function',
      location: '@pro-laico/core',
      description: "Produces a Next.js Metadata object from a page's meta fields and the site fallbacks (title, description, Open Graph image, favicons, robots).",
      params: {
        args: { type: '{ page?, siteMetadata? }', description: 'The page meta slice and the site-wide fallback metadata. Both are structural shapes, so any doc carrying the expected fields works.' },
      },
      returns: { type: 'Metadata', description: 'A finished Next.js Metadata object to return from `generateMetadata`.' },
      exampleLang: 'tsx',
      example: `import { GenerateMetaData } from '@pro-laico/core'

// app/(frontend)/[slug]/page.tsx
export async function generateMetadata({ params }) {
  const page = await getPage(params.slug)
  const siteMetadata = await getSiteMetadata()
  return GenerateMetaData({ page, siteMetadata })
}`,
    },
    generateLivePreviewPath: {
      type: 'function',
      location: '@pro-laico/core',
      description: 'Builds the URL Payload\'s admin live-preview iframe should hit. Pass it as a collection\'s `admin.livePreview.url`. The host project must also mount the `/next/preview` route handler.',
      params: {
        args: { type: '{ data, req }', description: 'The in-flight document `data` and the Payload `req`, as Payload passes them to the live-preview URL builder.' },
        'options?': { type: '{ pagesSlug?: string; fallbackPath?: string }', description: 'Override the pages collection slug (`pagesSlug`, defaults to `pages`) and the path the preview falls back to when the document has no resolvable href or `testPath` (`fallbackPath`, defaults to `/testing`). Set `fallbackPath` to point a global-like document or a single-page site at a fixed page, e.g. `(args) => generateLivePreviewPath(args, { fallbackPath: "/" })`.' },
      },
      returns: { type: 'Promise<string>', description: 'The absolute preview URL (falls back to `fallbackPath`, default `/testing`).' },
      example: `import { generateLivePreviewPath } from '@pro-laico/core'

export const Pages = {
  slug: 'pages',
  admin: {
    livePreview: { url: generateLivePreviewPath },
  },
}`,
    },
  }}
/>

### Utilities

<ExportTable
  exports={{
    mergeCollection: {
      type: 'function',
      location: '@pro-laico/core',
      description: 'Merge a partial override onto a base collection config without clobbering nested config. `access` / `admin` shallow-merge, `fields` append, `hooks` merge per-phase. The composition helper the plugins use internally to let you extend their collections.',
      params: {
        base: { type: 'CollectionConfig', description: 'The default collection to extend.' },
        'override?': { type: 'Partial<CollectionConfig>', description: 'Your additions. When undefined, `base` is returned unchanged.' },
      },
      returns: { type: 'CollectionConfig', description: 'The merged collection.' },
      example: `import { mergeCollection } from '@pro-laico/core'

const Pages = mergeCollection(basePages, {
  admin: { group: 'Content' },
  fields: [{ name: 'subtitle', type: 'text' }],
})`,
    },
    mergeGlobal: {
      type: 'function',
      location: '@pro-laico/core',
      description: 'The global-config counterpart of mergeCollection (globals have no `upload`). Shallow-merges `access` / `admin`, appends `fields`, merges `hooks` per-phase.',
      params: {
        base: { type: 'GlobalConfig', description: 'The default global to extend.' },
        'override?': { type: 'Partial<GlobalConfig>', description: 'Your additions. When undefined, `base` is returned unchanged.' },
      },
      returns: { type: 'GlobalConfig', description: 'The merged global.' },
      example: `import { mergeGlobal } from '@pro-laico/core'

const Settings = mergeGlobal(baseSettings, {
  fields: [{ name: 'announcement', type: 'text' }],
})`,
    },
    mergeHooks: {
      type: 'function',
      location: '@pro-laico/core',
      description: 'Additively merge two Payload hook maps: each per-phase array in `extra` is appended after the base array. Works for collection, global, and field hooks.',
      params: {
        base: { type: 'T', description: 'The base hooks object.' },
        'extra?': { type: 'T', description: 'Hooks to append per phase. When undefined, `base` is returned unchanged.' },
      },
      returns: { type: 'T', description: 'The merged hooks map (base hooks run before extra within each phase).' },
      example: `import { mergeHooks } from '@pro-laico/core'

const hooks = mergeHooks(
  { beforeChange: [baseHook] },
  { beforeChange: [myHook], afterDelete: [cleanup] },
)`,
    },
    deepMerge: {
      type: 'function',
      location: '@pro-laico/core',
      description: 'A deep object merge utility. Recursively merges `source` onto `target`, returning a new object.',
      params: {
        target: { type: 'T', description: 'The base object.' },
        source: { type: 'R', description: 'The object whose values win on conflict.' },
      },
      returns: { type: 'T', description: 'A new deeply-merged object.' },
      example: `import { deepMerge } from '@pro-laico/core'

const config = deepMerge(defaults, overrides)`,
    },
    sanitizeData: {
      type: 'function',
      location: '@pro-laico/core',
      description: 'Recursively removes undefined, null, empty arrays, and empty objects without mutating the input (rich-text children are left intact). Use it before passing Payload data to a client component.',
      params: {
        incomingData: { type: 'T', description: 'The data to clean.' },
      },
      returns: { type: 'T', description: 'A cleaned clone of the input.' },
      example: `import { sanitizeData } from '@pro-laico/core'

const clean = sanitizeData(doc)
return <ClientBlock data={clean} />`,
    },
    'string helpers': {
      type: 'function',
      location: '@pro-laico/core',
      description: 'toTitleCase and toKebabCase string-casing helpers, each taking a string and returning the recased string.',
      params: {
        value: { type: 'string', description: 'The string to recase.' },
      },
      returns: { type: 'string', description: 'The recased string.' },
      example: `import { toKebabCase, toTitleCase } from '@pro-laico/core'

toTitleCase('hero block') // 'Hero Block'
toKebabCase('Hero Block') // 'hero-block'`,
    },
    manualLogger: {
      type: 'function',
      location: '@pro-laico/core',
      description: 'A small tagged logger used across the packages. Colorizes by tag (`[INFO]`, `[WARNING]`, `[ERROR]`, …) and only prints when the LOGS env var is `true`.',
      params: {
        message: { type: 'string', description: 'The message, optionally prefixed with a recognized tag.' },
      },
      returns: { type: 'void', description: 'Logs to the console; returns nothing.' },
      example: `import { manualLogger } from '@pro-laico/core'

manualLogger('[INFO] Regenerated stylesheet')`,
    },
    revalidateTag: {
      type: 'function',
      location: '@pro-laico/core',
      description: 'A safe wrapper around Next.js revalidateTag, with the cross-tag fan-out the workspace relies on (saving `pages` also revalidates `sitemap`, etc.). Marked `use server`, so it is callable from a server action.',
      params: {
        tag: { type: 'string', description: 'The primary cache tag to revalidate.' },
        'id?': { type: 'string', description: 'An id to scope the tag to (id-bound entries like `image` / `page`).' },
        'draft?': { type: 'boolean', description: 'Revalidate the draft variant.' },
      },
      returns: { type: 'Promise<void>', description: 'Revalidates the derived tags (and, for some tags, their dependents).' },
      example: `'use server'
import { revalidateTag } from '@pro-laico/core'

export async function refreshPages() {
  await revalidateTag('pages', false)
}`,
    },
  }}
/>

### Cache

<ExportTable
  exports={{
    withCache: {
      type: 'function',
      location: '@pro-laico/core/cache',
      description: 'The caching primitive every package getter wraps its fetch with. Derives the unstable_cache key and dependency tags from a tag, id, and draft flag, so read-side tags match the revalidate side. Subpath-only because it imports server-only.',
      params: {
        fetcher: { type: '() => Promise<T>', description: 'The data fetch to cache (typically a Local API read).' },
        options: { type: 'WithCacheOptions', description: '`tag` (required), optional `tid` (id), `draft`, and `extraTags` for cross-tag dependencies.' },
      },
      returns: { type: 'Promise<T>', description: 'The fetcher result, cached and tagged.' },
      example: `import { withCache } from '@pro-laico/core/cache'
import { getPayloadInstance } from '@pro-laico/core/payload'

export const getCachedDesignSet = (draft: boolean) =>
  withCache(
    async () => {
      const payload = await getPayloadInstance()
      const { docs } = await payload.find({ collection: 'designSet', where: { active: { equals: true } }, draft })
      return docs[0]
    },
    { tag: 'designSet', draft },
  )`,
    },
    mt: {
      type: 'function',
      location: '@pro-laico/core/cache',
      description: 'Merge-tags helper that joins a tag, id, and draft flag into a single colon-delimited dependency-tag string (skipping empty parts). Used by withCache and when a getter needs extra cross-tag dependencies.',
      params: {
        stringArray: { type: '(string | number | undefined)[]', description: 'The parts to join (empty / nullish parts are dropped).' },
      },
      returns: { type: 'string', description: 'The colon-joined tag string (e.g. `image:42:draft`).' },
      example: `import { mt } from '@pro-laico/core/cache'

mt(['image', id, draft ? 'draft' : undefined]) // 'image:42:draft'`,
    },
  }}
/>

### Components

<ExportTable
  exports={{
    Toaster: {
      type: 'component',
      location: '@pro-laico/core/components/frontend/Toaster',
      description: 'The frontend toast / notification component. Mount it once in your root layout so server actions can surface toasts.',
      exampleLang: 'tsx',
      example: `import { Toaster } from '@pro-laico/core/components/frontend/Toaster'

// app/(frontend)/layout.tsx
export default function RootLayout({ children }) {
  return (
    <html>
      <body>
        {children}
        <Toaster />
      </body>
    </html>
  )
}`,
    },
    LivePreviewListener: {
      type: 'component',
      location: '@pro-laico/core/components/frontend/LivePreviewListener',
      description: 'A frontend component that refreshes the page during Payload live preview. Render it inside draft mode so admin edits reflect live.',
      exampleLang: 'tsx',
      example: `import { draftMode } from 'next/headers'
import { LivePreviewListener } from '@pro-laico/core/components/frontend/LivePreviewListener'

// app/(frontend)/layout.tsx
export default async function RootLayout({ children }) {
  const { isEnabled: draft } = await draftMode()
  return (
    <html>
      <body>
        {draft && <LivePreviewListener />}
        {children}
      </body>
    </html>
  )
}`,
    },
    SlugComponent: {
      type: 'component',
      location: '@pro-laico/core/ui/fields/slug',
      description: "The slug field admin UI (the default export, a client component). It renders the slug text input with a Lock / Unlock button that toggles the paired slugLock checkbox; while locked, the input is read-only and the slug stays auto-formatted from the watched field (the `fieldToUse` you gave `slugField`). `slugField` wires it in for you by pointing the field's admin component path at this subpath (the `SlugPath` constant) with the matching `clientProps`, so you rarely import it yourself.",
    },
    'siteTriggers component': { type: 'component', location: '@pro-laico/core/ui/root/siteTriggers', description: 'The admin component for the deploy / site triggers (referenced via SiteTriggersPath).' },
    'APF admin components': { type: 'component', location: '@pro-laico/core/admin/controls', description: 'The APF controls, field, and label admin components (also at /admin/field and /admin/label, referenced via the APF path constants).' },
    'path constants': { type: 'constant', location: '@pro-laico/core', description: "String paths Payload's importMap resolves to admin components: APFControlsPath, APFieldPath, APFieldLabelPath, SiteTriggersPath, and SlugPath." },
  }}
/>

### Kernel & config

<ExportTable
  exports={{
    'kernel types': { type: 'type', location: '@pro-laico/core', description: 'The type-indirection layer: the PayloadAugment interface, the Get<> lookup, the default fallbacks, and generic config helpers like CollectionBySlug.' },
    registerPayloadConfig: {
      type: 'function',
      location: '@pro-laico/core/config',
      description: 'Registers your Payload config so the cache helpers and getPayloadInstance can reach the Local API without importing @payload-config from package source. Call it once at startup, in your Next.js instrumentation hook.',
      params: {
        config: { type: 'PayloadConfigPromise', description: 'Your project config (or its resolution promise), typically `import configPromise from \'@payload-config\'`.' },
      },
      returns: { type: 'void', description: 'Stores the config on a process-global registry.' },
      example: `// src/instrumentation.ts
export async function register(): Promise<void> {
  if (process.env.NEXT_RUNTIME !== 'nodejs') return

  const { registerPayloadConfig } = await import('@pro-laico/core/config')
  const { default: configPromise } = await import('@payload-config')

  registerPayloadConfig(configPromise)
}`,
    },
    getPayloadInstance: {
      type: 'function',
      location: '@pro-laico/core/payload',
      description: 'Returns a memoized Local API Payload instance, resolved from the config registered via registerPayloadConfig. Use it in cache getters and server code instead of importing @payload-config directly. Takes no arguments.',
      returns: { type: 'Promise<Payload>', description: 'The host project\'s Payload instance.' },
      example: `import { getPayloadInstance } from '@pro-laico/core/payload'

const payload = await getPayloadInstance()
const { docs } = await payload.find({ collection: 'pages' })`,
    },
    createPreviewRouteHandler: {
      type: 'function',
      location: '@pro-laico/core/next/preview',
      description: 'Factory for the Next.js GET handler that enables Payload draft mode after validating the previewSecret query param. Mount it at the route generateLivePreviewPath points to.',
      params: {
        args: { type: '{ configPromise }', description: 'The host Payload config (or its promise), typically `import configPromise from \'@payload-config\'`.' },
      },
      returns: { type: '(req: NextRequest) => Promise<Response>', description: 'The route handler. Export it as `GET`.' },
      example: `// app/(frontend)/next/preview/route.ts
import configPromise from '@payload-config'
import { createPreviewRouteHandler } from '@pro-laico/core/next/preview'

export const GET = createPreviewRouteHandler({ configPromise })`,
    },
    exitPreviewRouteHandler: {
      type: 'function',
      location: '@pro-laico/core/next/exit-preview',
      description: 'The Next.js GET handler that disables Payload draft mode. Mount it alongside the preview route. It is the handler itself, not a factory.',
      returns: { type: 'Promise<Response>', description: 'Disables draft mode and returns a confirmation response.' },
      example: `// app/(frontend)/next/exit-preview/route.ts
import { exitPreviewRouteHandler } from '@pro-laico/core/next/exit-preview'

export const GET = exitPreviewRouteHandler`,
    },
  }}
/>

## Related

<Cards>
  <Card title="Architecture & the plugin model" href="/docs/concepts/architecture" />
  <Card title="The kernel: Get and PayloadAugment" href="/docs/concepts/kernel" />
  <Card title="Type augmentation" href="/docs/concepts/type-augmentation" />
  <Card title="Caching & revalidation" href="/docs/concepts/caching" />
  <Card title="@pro-laico/zap" href="/docs/plugins/zap" />
</Cards>
