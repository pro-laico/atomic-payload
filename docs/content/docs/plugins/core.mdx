---
title: "@pro-laico/core"
description: "The shared foundation every Atomic Payload package builds on — end-to-end type safety, tag-based caching and revalidation, JSON-schema type generation, and reusable admin and frontend building blocks."
---

`@pro-laico/core` is the shared foundation the rest of Atomic Payload is built on. You rarely install it on its own — the other plugins bring it along — but it's the common ground they all rely on: end-to-end type safety, tag-based caching with correct revalidation, and a set of reusable fields, hooks, and UI pieces. This page is mostly a reference for what it provides.

## Installation

<Tabs groupId="package-manager" items={['pnpm', 'npm', 'yarn']}>

<Tab value="pnpm">

```bash
pnpm add @pro-laico/core
```

</Tab>

<Tab value="npm">

```bash
npm install @pro-laico/core
```

</Tab>

<Tab value="yarn">

```bash
yarn add @pro-laico/core
```

</Tab>

</Tabs>

<Callout type="info">
  `@base-ui-components/react`, `@payloadcms/live-preview-react`, `@payloadcms/ui`, `next`, `payload`, `react`, and `server-only` are required peers — you already have them in a Payload + Next.js app. The sibling `@pro-laico/*` packages (`atomic`, `images`, `site`, `styles`, `tracking`) are optional peers: install only the ones you use.
</Callout>

## Setup

You usually get `@pro-laico/core` transitively, and the [atomic-payload template](/docs/templates/atomic-payload) wires up everything below for you. When you reach for it directly, there are two pieces worth knowing.

<Steps>

<Step>
### Wire the plugins into your Payload config

The package ships two Payload plugins. `revalidationPlugin` attaches cache-revalidation hooks to the collection and global slugs you name; `jsonSchemaPlugin` extends `payload generate:types` so block fields get accurate generated types. Add whichever you need to `buildConfig`:

```ts
import { buildConfig } from 'payload'
import { revalidationPlugin } from '@pro-laico/core'

export default buildConfig({
  plugins: [
    revalidationPlugin({
      collectionSlugs: ['pages'],
      deleteCollectionSlugs: ['pages'],
      globalSlugs: ['settings'],
    }),
  ],
})
```

The template registers `jsonSchemaPlugin` alongside it, passing the block lists each `@pro-laico` plugin contributes so the generated types stay precise.
</Step>

<Step>
### Generate types with the augmentation step

`@pro-laico/core` includes a `core-augment-types` command that writes a `payload-types.augment.d.ts` file next to your generated `payload-types.ts`. It connects your project's concrete generated types into the shared type system, so every plugin's schema stubs resolve to your real shapes instead of permissive fallbacks. Chain it after Payload's own type generation — this is exactly what the template's `generate:types` script does:

```json
{
  "scripts": {
    "generate:types": "payload generate:types && core-augment-types"
  }
}
```

The generated `payload-types.augment.d.ts` is rewritten every time you run this, so add it to your `.gitignore` next to the rest of the generated types:

```plaintext
# .gitignore — generated by payload generate:types
src/payload-types.ts
src/payload-types.augment.d.ts
```
</Step>

</Steps>

The rest of the package — fields, hooks, cache helpers, URL and metadata utilities, and the admin / frontend components — is imported directly where you need it. See the [Exports](#exports) table for the full surface and the import path for each item.

## Options

### `revalidationPlugin(options?)`

<TypeTable
  type={{
    enabled: { type: 'boolean', default: 'true', description: 'Set false to turn the plugin off (it becomes a no-op).' },
    collectionSlugs: { type: 'string[]', default: '[]', description: 'Collections that should revalidate cached content when a document changes (a beforeChange hook).' },
    deleteCollectionSlugs: { type: 'string[]', default: '[]', description: 'Collections that should revalidate cached content when a document is deleted (an afterDelete hook).' },
    globalSlugs: { type: 'string[]', default: '[]', description: 'Globals that should revalidate cached content when they change (a beforeChange hook).' },
  }}
/>

### `jsonSchemaPlugin(options)`

<TypeTable
  type={{
    enabled: { type: 'boolean', default: 'true', description: 'Set false to turn the plugin off (it becomes a no-op).' },
    toJSONSchemaExtensions: { type: 'ToJSONSchemaExtensionsFn', description: 'The base schema-extension function (the template uses the one from @pro-laico/zap).' },
    generateBlocksType: { type: 'GenerateBlocksTypeFn', description: 'Builds a JSON-schema definition for a named set of block references (also from @pro-laico/zap).' },
    blocks: { type: 'AtomicPayloadSchemaBlocks', description: 'The block reference lists each plugin contributes (child blocks, action blocks, form blocks, and so on) so their generated types are precise.' },
    extraDefinitions: { type: 'Record<string, JSONSchema4>', description: 'Optional per-project schema definitions, merged in last so they can override the defaults.' },
  }}
/>

## Environment variables

`@pro-laico/core`'s URL and preview helpers read these when present. They're optional — each has a sensible fallback — but setting them keeps generated links and preview routes pointing at the right place.

| Variable | Purpose |
| --- | --- |
| `NEXT_PUBLIC_SERVER_URL` | The site's base URL, used by `getServerSideURL` / `getClientSideURL` for absolute links and metadata. Falls back to `http://localhost:3000`. |
| `VERCEL_PROJECT_PRODUCTION_URL` | Used as the base URL on Vercel when `NEXT_PUBLIC_SERVER_URL` isn't set. |
| `PREVIEW_SECRET` | Validates draft-preview requests handled by the preview route handlers and `generateLivePreviewPath`. |
| `VERCEL_DEPLOY_WEBHOOK_URL` | The deploy hook the admin "trigger deploy" control posts to, when you use the site triggers component. |

## Exports

<ExportTable
  exports={{
    revalidationPlugin: { type: 'plugin', location: '@pro-laico/core', description: 'Attaches the cache-revalidation hooks to the collection and global slugs you list. Default and named export.' },
    RevalidationPluginOptions: { type: 'type', location: '@pro-laico/core', description: "The TypeScript type for revalidationPlugin's options." },
    jsonSchemaPlugin: { type: 'plugin', location: '@pro-laico/core', description: 'Extends payload generate:types with accurate JSON-schema definitions for block fields.' },
    'jsonSchemaPlugin options + types': { type: 'type', location: '@pro-laico/core', description: 'JSONSchemaPluginOptions, AtomicPayloadSchemaBlocks, BlockRefs, and the related schema-extension function types.' },
    createJSONSchemaExtensions: { type: 'function', location: '@pro-laico/core', description: 'Builds the schema-extension function jsonSchemaPlugin appends — call it directly to wire schema generation by hand.' },
    atomicPayloadStoredDefinitions: { type: 'object', location: '@pro-laico/core', description: 'The shared JSON-schema definitions for stored Atomic forms and actions, merged into generated types.' },
    slugField: { type: 'field', location: '@pro-laico/core', description: 'A reusable slug field with the auto-formatting hook attached.' },
    StorageTab: { type: 'field', location: '@pro-laico/core', description: 'The admin tab that holds generated CSS / storage fields.' },
    DevModeField: { type: 'field', location: '@pro-laico/core', description: 'A field that surfaces developer-only controls in the admin.' },
    'TestPathField / createTestPathField': { type: 'field', location: '@pro-laico/core', description: 'A field (and its factory) for the live-preview test path.' },
    UniqueTitleField: { type: 'field', location: '@pro-laico/core', description: 'A title field that enforces uniqueness.' },
    ActiveField: { type: 'field', location: '@pro-laico/core', description: 'A reusable active / enabled toggle field.' },
    'APField + APF helpers': { type: 'function', location: '@pro-laico/core', description: 'The Atomic Payload Functions field plus its registry, storage, generators, and runAPF runner — the building blocks for configurable block behavior.' },
    'revalidate hooks': { type: 'function', location: '@pro-laico/core', description: 'The collection and global revalidation hooks and their default handler maps (revalidateCacheCollection, revalidateCacheOnDelete, revalidateCacheGlobal, and friends) — wired for you by revalidationPlugin.' },
    sanitizeAfterRead: { type: 'function', location: '@pro-laico/core', description: 'An afterRead collection hook that strips internal fields from API responses.' },
    'formatSlug / formatSlugHook': { type: 'function', location: '@pro-laico/core', description: 'Turns a title into a URL-safe slug, as a plain helper and as a field hook.' },
    updateHrefHook: { type: 'function', location: '@pro-laico/core', description: 'Keeps a document href field in sync as its slug or parents change.' },
    updatePublishedAtHook: { type: 'function', location: '@pro-laico/core', description: 'Stamps publishedAt the first time a document is published.' },
    getServerSideURL: { type: 'function', location: '@pro-laico/core', description: 'Resolves the site base URL on the server (reads NEXT_PUBLIC_SERVER_URL with sensible fallbacks).' },
    getClientSideURL: { type: 'function', location: '@pro-laico/core', description: 'Resolves the site base URL in the browser, falling back to env vars during SSR.' },
    getImageUrl: { type: 'function', location: '@pro-laico/core', description: 'Builds an absolute URL for an uploaded image.' },
    GenerateMetaData: { type: 'function', location: '@pro-laico/core', description: "Produces a Next.js metadata object from a document's SEO fields." },
    generateLivePreviewPath: { type: 'function', location: '@pro-laico/core', description: 'Builds the preview URL used by Payload live preview.' },
    'mergeCollection / mergeGlobal': { type: 'function', location: '@pro-laico/core', description: 'Merge user overrides onto a default collection or global config — the composition helpers every plugin uses internally.' },
    mergeHooks: { type: 'function', location: '@pro-laico/core', description: 'Combine multiple hook maps into one.' },
    deepMerge: { type: 'function', location: '@pro-laico/core', description: 'A deep object merge utility (with the isObject guard).' },
    sanitizeData: { type: 'function', location: '@pro-laico/core', description: 'Strips non-serializable values before passing data to the client.' },
    'string + duration helpers': { type: 'function', location: '@pro-laico/core', description: 'toTitleCase, toKebabCase, formatDurationString, and the mt merge-tags helper.' },
    manualLogger: { type: 'function', location: '@pro-laico/core', description: 'A small logger used across the packages.' },
    revalidateTag: { type: 'function', location: '@pro-laico/core', description: 'A safe wrapper around Next.js revalidateTag for use in server actions.' },
    'path constants': { type: 'constant', location: '@pro-laico/core', description: "String paths Payload's importMap resolves to admin components: APFControlsPath, APFieldPath, APFieldLabelPath, SiteTriggersPath, and SlugPath." },
    'kernel types': { type: 'type', location: '@pro-laico/core/kernel', description: 'The type-indirection layer: the PayloadAugment interface, the Get<> lookup, the default fallbacks, and generic config helpers like CollectionBySlug.' },
    registerPayloadConfig: { type: 'function', location: '@pro-laico/core/config', description: 'Registers your Payload config so the cache helpers can reach the Local API — called once from the Next.js instrumentation hook.' },
    'cache factories': { type: 'function', location: '@pro-laico/core/cache', description: 'createGetCached / createDefaultGetCached — server-side, tag-based cached read factories. Subpath-only because they import server-only.' },
    'createReactCachedGetCached': { type: 'function', location: '@pro-laico/core/cache/react', description: 'A React cache() wrapper around the cached read factory.' },
    'auto getCached': { type: 'function', location: '@pro-laico/core/cache/auto', description: 'An auto-keyed cached read, default-exported — what the template imports as getCached in pages and the layout.' },
    revalidationLogger: { type: 'function', location: '@pro-laico/core/logger', description: 'A server-only logger for revalidation events. Subpath-only because it imports server-only.' },
    getPayloadInstance: { type: 'function', location: '@pro-laico/core/payload', description: 'Returns a memoized Local API Payload instance.' },
    getMeUser: { type: 'function', location: '@pro-laico/core/auth/getMeUser', description: 'Resolves the currently logged-in user on the frontend. Subpath-only because it imports next/navigation.' },
    'preview route handlers': { type: 'function', location: '@pro-laico/core/next/preview', description: 'createPreviewRouteHandler and exitPreviewRouteHandler (from @pro-laico/core/next/exit-preview) — the GET handlers for the draft-preview routes.' },
    Toaster: { type: 'component', location: '@pro-laico/core/components/frontend/Toaster', description: 'The frontend toast/notification component.' },
    LivePreviewListener: { type: 'component', location: '@pro-laico/core/components/frontend/LivePreviewListener', description: 'A frontend component that refreshes the page during Payload live preview.' },
    'slug field component': { type: 'component', location: '@pro-laico/core/ui/fields/slug', description: 'The admin UI component for the slug field (referenced via SlugPath).' },
    'siteTriggers component': { type: 'component', location: '@pro-laico/core/ui/root/siteTriggers', description: 'The admin component for the deploy / site triggers (referenced via SiteTriggersPath).' },
    'APF admin components': { type: 'component', location: '@pro-laico/core/admin/controls', description: 'The APF controls, field, and label admin components (also at /admin/field and /admin/label, referenced via the APF path constants).' },
  }}
/>

## Related

<Cards>
  <Card title="Architecture & the plugin model" href="/docs/concepts/architecture" />
  <Card title="The kernel: Get and PayloadAugment" href="/docs/concepts/kernel" />
  <Card title="Type augmentation" href="/docs/concepts/type-augmentation" />
  <Card title="Caching & revalidation" href="/docs/concepts/caching" />
  <Card title="@pro-laico/zap" href="/docs/plugins/zap" />
</Cards>
