---
title: "@pro-laico/fonts"
description: "Manage custom fonts in the Payload admin and use them with next/font/local — upload your fonts, pick the active ones, and a build step delivers them to your app."
---

`@pro-laico/fonts` lets you manage custom fonts from the Payload admin and use them with `next/font/local`. Upload your font files, choose the active sans / serif / mono / display faces in the dashboard, and a build step pulls them into your app — no font files hardcoded in your repo.

## Installation

<Tabs groupId="package-manager" items={['pnpm', 'npm', 'yarn']}>

<Tab value="pnpm">

```bash
pnpm add @pro-laico/fonts
```

</Tab>

<Tab value="npm">

```bash
npm install @pro-laico/fonts
```

</Tab>

<Tab value="yarn">

```bash
yarn add @pro-laico/fonts
```

</Tab>

</Tabs>

<Callout type="info">
  `payload` is a peer you already have in a Payload app. `@payloadcms/sdk`, `dotenv`, and `tsx` are only needed when you run the font download step (below) — install them if you don't have them yet.
</Callout>

## Setup

<Tabs items={['Standalone', 'With the atomic-payload template']}>

<Tab value="Standalone">

Adding fonts to your own Payload + Next.js project.

<Steps>

<Step>
### Add the plugin to your Payload config

```ts
import { buildConfig } from 'payload'
import { fontsPlugin } from '@pro-laico/fonts'

export default buildConfig({
  plugins: [fontsPlugin({ includeFontSet: true })],
})
```

This registers the **Font** collection (where you upload font files) and the **`fontSet`** global (where you choose the active font for each role: sans / serif / mono / display).

<Callout type="info">
  Already using [`@pro-laico/styles`](/docs/plugins/styles)? Leave `includeFontSet` off and pick fonts in the design set's **Fonts** tab instead — that's how the template does it (see the other tab).
</Callout>
</Step>

<Step>
### Upload and choose your fonts in the admin

1. Open the **Font** collection and upload your font files (`.woff`, `.woff2`, `.ttf`, or `.otf`).
2. In the **`fontSet`** global, pick the active font for each role — sans, serif, mono, and display.
</Step>

<Step>
### Add the font download step to your build

`next/font/local` reads fonts from files in your project, not from the CMS — so you need a step that copies the chosen fonts into your project before each build. Add it to your `package.json` and wire it as a `prebuild` script so it runs automatically every time you build:

```json
{
  "scripts": {
    "prebuild": "pnpm generate:fonts",
    "generate:fonts": "pnpm exec tsx node_modules/@pro-laico/fonts/src/scripts/cli.ts"
  }
}
```

Running it needs `tsx` installed (a dev dependency). When it runs, it signs in to a running Payload instance (your deploy, or a local dev server), looks up the active fonts, and:

- Downloads the active font files into `public/fonts`.
- Writes a small generated file at `src/app/definition.ts` that hooks those fonts into `next/font/local`.

Because it reads a running Payload instance, it needs a few [environment variables](#environment-variables) and a reachable site. It runs on every build; you can also run it any time with `pnpm generate:fonts` after changing the active fonts. Working locally with nothing deployed yet? See [Notes](#notes) for a one-command wrapper.
</Step>

<Step>
### Ignore the generated files

The download step regenerates the font files and the definition module on every build, so they don't belong in version control. Add them to your `.gitignore`:

```plaintext
# .gitignore — generated by @pro-laico/fonts
public/fonts
src/app/definition.ts
```
</Step>

<Step>
### Use the fonts in your app

The generated `definition.ts` default-exports your fonts as `next/font/local` objects, each carrying a CSS variable (`--font-setSans`, `--font-setSerif`, and so on). Pass that export through the package's `extractFonts` helper to get the combined `className` for your root `<html>` — it returns `undefined` until the fonts are generated — and the rest of your styles can reference the variables:

```tsx
// app/(frontend)/layout.tsx
import { extractFonts } from '@pro-laico/fonts'

import fonts from '@/app/definition'

export default function RootLayout({ children }: { children: React.ReactNode }) {
  const fontVariables = extractFonts(fonts)

  return (
    <html lang="en" className={fontVariables}>
      <body>{children}</body>
    </html>
  )
}
```

With the variables in place, your design set (or your own CSS) can point Tailwind's font families at them — `--font-sans: var(--font-setSans)` — so picking a different font in the admin restyles the whole site.
</Step>

</Steps>

</Tab>

<Tab value="With the atomic-payload template">

The [atomic-payload template](/docs/templates/atomic-payload) already wires everything up — the **Font** collection, the build step (`prebuild` → `generate:fonts`), and the layout. You just choose your fonts and build.

<Steps>

<Step>
### Upload your fonts

Open the **Font** collection in the admin and upload your font files (`.woff`, `.woff2`, `.ttf`, or `.otf`).
</Step>

<Step>
### Pick the active fonts

In the active **design set**, open the **Fonts** tab and choose the font for each role — sans, serif, mono, and display.
</Step>

<Step>
### Build (or refresh)

The template's `prebuild` script runs the font download automatically on `next build`, so a normal build picks up your choices. To refresh during development after changing the active fonts, run it on demand:

```bash
pnpm generate:fonts
```

It needs the [environment variables](#environment-variables) below set, so it can read your fonts from the deployed site.
</Step>

</Steps>

</Tab>

</Tabs>

## Options

`fontsPlugin(options?)` accepts:

<TypeTable
  type={{
    enabled: { type: 'boolean', default: 'true', description: 'Set false to turn the plugin off.' },
    includeFontSet: { type: 'boolean', default: 'false', description: 'When true, registers the fontSet global where you choose the active sans / serif / mono / display fonts.' },
    fontOptions: { type: 'Partial<CollectionConfig>', description: 'Tweaks merged onto the Font collection — e.g. pin a custom upload folder or add fields. Upload, access, and hooks merge onto the defaults; fields are appended.' },
    fontSetOptions: { type: 'Partial<GlobalConfig>', description: 'Tweaks merged onto the fontSet global (when includeFontSet is true) — extra fields are appended and access is merged.' },
  }}
/>

## Environment variables

The download step reads your live site to find the active fonts and pull the files, using:

| Variable | Purpose |
| --- | --- |
| `LIVE_SITE_URL` | The site to read the active fonts from, and the base URL the files are downloaded from. |
| `SCRIPT_USER_EMAIL` | A Payload user to sign in as (so it can read your fonts). |
| `SCRIPT_USER_PASSWORD` | That user's password. |

You can change where the files and the generated module go with optional overrides — `ATOMIC_FONTS_OUTPUT_DIR` (default `./public/fonts`), `ATOMIC_FONTS_DEFINITION_FILE` (default `./src/app/definition.ts`), and `ATOMIC_FONTS_CSS_VAR_PREFIX` (default `--font-set`), among others.

## Notes

`generate:fonts` reads the active fonts from a **running, reachable Payload instance** — it signs in to `LIVE_SITE_URL` over HTTP and downloads each font from the URL Payload reports, so it can't read them offline. In CI or on a deploy, point `LIVE_SITE_URL` at your deployed site and the `prebuild` script runs it on every build.

For **local development**, before anything's deployed, add a one-command wrapper that boots a temporary dev server, runs the download against it, then shuts it down:

```json
{
  "scripts": {
    "setup:fonts": "cross-env LIVE_SITE_URL=http://localhost:3000 start-server-and-test dev http://localhost:3000 generate:fonts"
  }
}
```

It needs `start-server-and-test` and `cross-env` as dev dependencies. Set `SCRIPT_USER_EMAIL` / `SCRIPT_USER_PASSWORD` in `.env` (an admin user that can read your fonts), then run `pnpm setup:fonts`. The [`fonts-only` example](/docs/examples/fonts-only) wires this up.

## Exports

<ExportTable
  exports={{
    fontsPlugin: { type: 'plugin', location: '@pro-laico/fonts', description: 'The plugin itself — registers the Font collection and, with includeFontSet, the fontSet global. Default and named export.' },
    FontsPluginOptions: { type: 'type', location: '@pro-laico/fonts', description: "The TypeScript type for the plugin's options." },
    Font: { type: 'collection', location: '@pro-laico/fonts', description: 'The upload collection your font files live in — import it to register or extend the collection yourself.' },
    FontSet: { type: 'global', location: '@pro-laico/fonts', description: 'The standalone font-selection global (active sans / serif / mono / display) that includeFontSet registers.' },
    createFontSetGlobal: { type: 'function', location: '@pro-laico/fonts', description: 'Factory for a customized fontSet global — use it when you need to change its slug or fields.' },
    FONT_SET_SLUG: { type: 'constant', location: '@pro-laico/fonts', description: 'The slug string for the fontSet global ("fontSet") — handy when querying it.' },
    fontUploadField: { type: 'function', location: '@pro-laico/fonts', description: "Builds the font group field for a @pro-laico/styles design set's Fonts tab." },
    fontUploadFields: { type: 'function', location: '@pro-laico/fonts', description: 'Builds the four upload slot fields (sans / serif / mono / display) shared by the global and the design-set group.' },
    extractFonts: { type: 'function', location: '@pro-laico/fonts', description: 'Turns the generated definition.ts export into the space-separated next/font className for your root <html> — returns undefined before the fonts are generated.' },
    runDownloadFonts: { type: 'function', location: '@pro-laico/fonts/scripts/downloadFonts', description: 'The font download step as a function — call it from your own build script instead of the generate:fonts command.' },
    'Font / FontSet (types)': { type: 'type', location: '@pro-laico/fonts/schema', description: 'TypeScript types for typed access to your font data and the active selection.' },
  }}
/>

## Related

<Cards>
  <Card title="Fonts" href="/docs/features/fonts" />
</Cards>
