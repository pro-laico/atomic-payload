---
title: "@pro-laico/icons"
description: "Manage your SVG icons in the Payload admin: upload them, group them into reusable sets, and render any icon by name on your site."
---

`@pro-laico/icons` makes your SVG icons part of the CMS. Upload your own SVGs in the Payload admin and they're optimized and cleaned up automatically, ready for the frontend. Group them into named sets, mark one active, and render any icon by name on your site. It's as easy to use as a public icon library like Lucide, but the icons are yours, with no fixed catalog to live inside.

## Installation

<Tabs groupId="package-manager" items={['pnpm', 'npm', 'yarn']}>

<Tab value="pnpm">

```bash
pnpm add @pro-laico/icons
```

</Tab>

<Tab value="npm">

```bash
npm install @pro-laico/icons
```

</Tab>

<Tab value="yarn">

```bash
yarn add @pro-laico/icons
```

</Tab>

</Tabs>

<Callout type="info">
  `payload`, `next`, `react`, `@payloadcms/ui`, and `server-only` are peers you already have in a Payload + Next.js app. `svgo` and `svg-path-bbox` are **optional**: they power the upload-time cleanup and `viewBox` tightening, so install them if you want SVGs optimized on upload.
</Callout>

## Setup

<Tabs items={['Standalone', 'With the atomic-payload template']}>

<Tab value="Standalone">

Adding icons to your own Payload + Next.js project.

<Callout type="info">
  `@pro-laico/core` comes bundled with this plugin — it's a dependency, not a separate install. Its cached reads reach Payload through a config you register once in your Next.js instrumentation hook (`registerPayloadConfig`); see [@pro-laico/core → Setup](/docs/plugins/core#setup) to wire it up.
</Callout>

<Steps>

<Step>
### Add the plugin to your Payload config

```ts
import { buildConfig } from 'payload'
import { iconsPlugin } from '@pro-laico/icons'

export default buildConfig({
  plugins: [iconsPlugin()],
})
```

This registers two collections: **Icon** (where you upload SVG files) and **IconSet** (where you group icons under a named set and mark one set active). To register only the upload collection and skip the grouping concept, pass `includeIconSet: false`.

<Callout type="info">
  Icon sets get live preview by default: `iconSetOptions.livePreviewUrl` defaults to `@pro-laico/core`'s `generateLivePreviewPath` (built from `PREVIEW_SECRET` + `NEXT_PUBLIC_SERVER_URL`). Pass your own `iconSetOptions.livePreviewUrl` to override it or point preview elsewhere (see [Options](#options)).
</Callout>
</Step>

<Step>
### Regenerate the admin import map

The IconSet editor uses a custom row label component that Payload loads through its import map. After adding the plugin, regenerate the map so the admin can find it:

```bash
pnpm payload generate:importmap
```

Run this again whenever you add or remove plugins that contribute admin components.
</Step>

<Step>
### Upload icons and build your sets in the admin

1. Open the **Icon** collection and upload your SVG files. Each upload is cleaned up and its `viewBox` tightened automatically.
2. Open the **IconSet** collection, create a named set (for example "Nav" or "Social"), add icons to it by name, and mark the set **active**. Only one set is active at a time, and that's the set your site renders from.
</Step>

<Step>
### Render icons in your app

Render an icon by name from the active set with the `<Icon>` server component, imported from the `/Icon` subpath:

```tsx
import { Icon } from '@pro-laico/icons/Icon'

export function Toolbar() {
  return (
    <nav>
      <Icon name="arrow-right" />
      <Icon name="arrow-right" className="size-6 text-primary" />
      <Icon name="logo" fallback={myCustomSvgString} />
    </nav>
  )
}
```

`name` looks the icon up in the active icon set; any JSX props you pass (`className`, `style`, `width`, …) are applied to the rendered `<svg>` and win over the source's intrinsic attributes; `fallback` is a raw SVG string used when no icon matches the name (otherwise a small warning glyph shows). Because lookups go through `@pro-laico/core`'s cached getters, swapping the active set or editing one icon in the admin invalidates just the affected `<svg>`, not the whole page.
</Step>

</Steps>

</Tab>

<Tab value="With the atomic-payload template">

The [atomic-payload template](/docs/templates/atomic-payload) already registers the **Icon** and **IconSet** collections (with live preview wired up) and includes the import map. You just manage your icons and render them.

<Steps>

<Step>
### Upload and group your icons

Open the **Icon** collection and upload your SVG files, then open the **IconSet** collection, create a named set, add your icons to it, and mark the set **active**.
</Step>

<Step>
### Place icons in content

The template registers the **IconChild** and **SVGChild** content blocks through `@pro-laico/atomic`'s children system, so editors can drop an icon (picked from the active set) or paste raw SVG straight into a page's content, no code needed.
</Step>

<Step>
### Render icons in code

To render an icon by name from your own components, use the bundled `<Icon>` server component:

```tsx
import { Icon } from '@pro-laico/icons/Icon'

<Icon name="arrow-right" />
<Icon name="arrow-right" className="size-6 text-primary" />
```

It resolves from the active icon set, so switching the active set in the admin restyles every icon your site renders by that name.
</Step>

</Steps>

</Tab>

</Tabs>

## Using it in your app

The package's main frontend piece is the `<Icon>` server component at the `@pro-laico/icons/Icon` subpath. It looks an icon up **by name** in the currently active icon set and inlines it as an `<svg>`. Render it from inside a server component, the way the package's own block renders it:

```tsx
import { Icon } from '@pro-laico/icons/Icon'

// A real server component: pass each icon's name from the active set.
export function SiteFooter() {
  return (
    <footer>
      <a href="https://github.com/your-org" aria-label="GitHub">
        <Icon name="github" className="size-6" />
      </a>
      <Icon name="logo" fallback={myCustomSvgString} />
    </footer>
  )
}
```

Because resolution is bound to the active `IconSet`, picking a different set in the admin re-routes every `<Icon>` to the new artwork without a code change. Keep icon names consistent across your sets so they stay interchangeable. If a name isn't found in the active set, the `fallback` SVG (or a built-in warning glyph) renders instead.

In a project that uses `@pro-laico/atomic`'s children system, editors can also place icons in content through two blocks: **IconChild** (picks an icon by name from the active set) and **SVGChild** (pastes raw SVG inline). You add them to your children blocks list by their default block exports, and they render through `@pro-laico/atomic/children`:

```ts
// The children blocks your atomic config exposes to editors.
import { Icon as IconChildBlock } from '@pro-laico/icons/blocks/iconChild'
import { SVGBlock } from '@pro-laico/icons/blocks/svgChild'

const ChildBlocks = [IconChildBlock, SVGBlock /* …other children… */]
```

Need a className field (or other fields) on the block? Build a customized one with the `createIconBlock` / `createSvgBlock` factories instead of the default export, and spread your fields in via `prependFields` / `appendFields`.

## Requested-icons usage panel

Icon names live in the CMS, so nothing connects the names your code asks for to the names a set actually defines. A typo in `<Icon name="chevvron" />` just falls back at render time, and nobody knows which names the code really needs. The usage panel closes that loop: a build-time scan collects the names your source requests, an opt-in admin panel diffs them against the set you're editing, and optional runtime tracking adds what production actually requested. The three setup steps build on each other, so you can stop after step one or go all the way.

<Steps>

<Step>
### Scan your source for icon names

The `atomic-icons-scan` command (published as the package's command) walks your source tree, finds every literal `<Icon name="...">` usage, and writes `icon-usage-manifest.json`. Run it as a prebuild script or CI step so the manifest stays current:

```bash
# scan ./src and ./app, write icon-usage-manifest.json
npx atomic-icons-scan

# explicit roots, output path, component names, and extensions
npx atomic-icons-scan src app --out icon-usage-manifest.json --component Icon --ext tsx,jsx
```

Only string literals are collected: `name="x"`, `name='x'`, `name={"x"}`, and a non-interpolated `` name={`x`} ``. A dynamic `name={expr}` (a variable or interpolated value) is intentionally skipped, since a static scan cannot know its value. The names are matched exactly against each icon's kebab-cased `name`, the same way `<Icon>` resolves at runtime.

You can also run the scan programmatically, for example from your own build script:

```ts
import { scanIconUsages, writeIconUsageManifest, resolveManifestPath } from '@pro-laico/icons/scan'

const { manifest } = scanIconUsages({ roots: ['src', 'app'] })
writeIconUsageManifest(manifest, resolveManifestPath())
```
</Step>

<Step>
### Show the panel in the admin

Set `iconSetOptions.usagePanel: true` to add the "Requested icons" panel to the IconSet edit view. It reads the manifest server-side and diffs it live against the icons in the set you're editing, flagging the missing names with their `file:line`:

```ts
import { iconsPlugin } from '@pro-laico/icons'

iconsPlugin({
  iconSetOptions: {
    usagePanel: true,
    // optional: defaults to ICON_USAGE_MANIFEST, then icon-usage-manifest.json at cwd
    usageManifestPath: 'icon-usage-manifest.json',
  },
})
```

The manifest path resolves in order: the `usageManifestPath` option, then the `ICON_USAGE_MANIFEST` env var, then `icon-usage-manifest.json` in the server's working directory. When no manifest exists yet, the panel renders a hint telling editors to run the scan.
</Step>

<Step>
### Track runtime misses (live)

The static scan only sees string literals. To also catch what production actually requests, including dynamic `<Icon name={slug} />` and genuine misses, set `trackRequests: true`:

```ts
import { iconsPlugin } from '@pro-laico/icons'

iconsPlugin({
  iconSetOptions: { usagePanel: true },
  trackRequests: true,
})
```

This registers the `iconRequest` collection and makes `<Icon>` record every name that fails to resolve against the active set. The recorder is throttled (at most once per name per minute per process), deferred via Next's `after()` so it never blocks the response, and best-effort (any failure is swallowed). These live misses, including the dynamic `name={slug}` ones the static scan can't see, show up in the panel's "Missing from this set" list with a live count and last-seen date. The `iconRequest` collection is hidden from the admin nav, since it's surfaced through the panel instead, which also has a "Clear runtime requests" button to wipe it.

<Callout type="info">
  The runtime recorder is on whenever `trackRequests` is true. To turn it off without changing config (for example in a specific environment), set the `ICON_USAGE_TRACKING=false` env var.
</Callout>

With all three in place, the panel becomes a three-way compare: names requested in code (the manifest) versus requested in production (`iconRequest`) versus defined in the set you're editing, so missing, dynamic-only, and present names are all visible at a glance.
</Step>

</Steps>

## Options

`iconsPlugin(options?)` accepts two top-level toggles plus two additive option bags, one for each collection. Your own hooks always run **after** the built-ins, so the SVG cleanup and cache-revalidation hooks always run first.

<TypeTable
  type={{
    enabled: { type: 'boolean', default: 'true', description: 'Set false to turn the plugin off: neither Icon nor IconSet is registered. Useful behind a feature flag.' },
    includeIconSet: { type: 'boolean', default: 'true', description: 'When false, only the Icon upload collection is registered and the IconSet grouping collection is skipped.' },
    iconOptions: { type: 'IconCollectionOptions', description: 'Tweaks for the Icon collection: appended fields, additive hooks, and a final shallow-merge escape hatch. See its keys below.' },
    iconSetOptions: { type: 'IconSetCollectionOptions', description: 'Tweaks for the IconSet collection: live preview, additive hooks, set-level fields, per-icon-row fields, the requested-icons panel, and admin label overrides. See its keys below.' },
    trackRequests: { type: 'boolean', default: 'false', description: 'When true, registers the diagnostic `iconRequest` collection AND makes `<Icon>` record every name that fails to resolve at runtime (throttled to once per name per minute per process, deferred via Next’s `after()` so it never blocks the response, and fire-and-forget). Force-disable the recorder with the `ICON_USAGE_TRACKING=false` env var. See the Requested-icons usage panel section.' },
    iconRequestOptions: { type: 'IconRequestCollectionOptions', description: 'Additive extension points (admin group, extra fields, hooks) for the `iconRequest` collection. Only meaningful when `trackRequests` is true.' },
  }}
/>

The `iconOptions` and `iconSetOptions` options take their own nested keys:

<Accordions>

<Accordion title="iconOptions keys">

Extension points for the **Icon** upload collection. All three are additive on top of the built-in defaults (slug `icon`, authenticated access, `image/svg+xml` upload, the SVG-optimizing `beforeChange` hook, and cache revalidation on save + delete).

<TypeTable
  type={{
    fields: { type: 'Field[]', default: '[]', description: 'Extra fields appended after the built-in optimized + svgString fields. Icon is a flat upload collection, so additions just stack.' },
    hooks: { type: "CollectionConfig['hooks']", description: 'Extra hooks merged in after the built-in cleanup and revalidation hooks, so your hooks always see the already-optimized SVG.' },
    collection: { type: 'Partial<CollectionConfig>', description: 'Escape hatch shallow-merged onto the collection last. Use for non-additive overrides like admin, access, upload, or versions.' },
  }}
/>

</Accordion>

<Accordion title="iconSetOptions keys">

Extension points for the **IconSet** collection. Each field-injection option lands in a different spot in the admin, so pick the one that matches the field's shape.

<TypeTable
  type={{
    livePreviewUrl: { type: '(args) => string | Promise<string>', default: "core's generateLivePreviewPath", description: 'Builds the live preview URL for IconSet edits, wiring both the legacy iframe preview and Payload 3 live preview. Live preview is on by default: this defaults to @pro-laico/core’s generateLivePreviewPath (built from PREVIEW_SECRET + NEXT_PUBLIC_SERVER_URL). Pass your own to override it or point preview elsewhere.' },
    extraSettingsFields: { type: 'Field[]', default: '[]', description: 'Compact fields packed INTO the row next to the title and active toggle. Best for narrow fields (booleans, short selects, slug-style text).' },
    fields: { type: 'Field[]', default: '[]', description: 'Full-width, set-level fields appended below the title/active row. Use for descriptions, relationships, and arrays that need more horizontal space.' },
    iconRowFields: { type: 'Field[]', default: '[]', description: 'Per-icon fields appended to each row in the set, after the existing name + icon fields (e.g. aliases, category).' },
    hooks: { type: "CollectionConfig['hooks']", description: 'Extra hooks merged in after the built-in revalidation hooks. This is also where you opt into the atomicHook snapshot behavior, via hooks.beforeChange.' },
    usagePanel: { type: 'boolean', default: 'false', description: 'Adds the "Requested icons" panel to the IconSet edit view: a live diff between the icon names your repo requests (collected by the build-time scan into a usage manifest) and the names defined in this set, with the missing ones flagged by their file:line. See the Requested-icons usage panel section.' },
    usageManifestPath: { type: 'string', description: 'Path to the usage manifest the panel reads. Falls back to the `ICON_USAGE_MANIFEST` env var, then `icon-usage-manifest.json` resolved against the server’s working directory. Only meaningful when usagePanel is true.' },
    usageScanCommand: { type: 'string', default: "'npx atomic-icons-scan'", description: 'Command shown in the panel’s empty state, telling editors how to generate the manifest. Only meaningful when usagePanel is true.' },
    useAsTitle: { type: 'string', default: "'title'", description: 'Field shown as the document title in the admin.' },
    group: { type: 'string', default: "'Sets'", description: 'Sidebar group label for the IconSet collection.' },
  }}
/>

</Accordion>

</Accordions>

All options at their defaults, as a working starting point:

```ts
import { generateLivePreviewPath } from '@pro-laico/core'

iconsPlugin({
  enabled: true,
  includeIconSet: true,
  iconOptions: {
    fields: [],
    // hooks and collection are unset
  },
  iconSetOptions: {
    // livePreviewUrl defaults to core's generateLivePreviewPath, so live preview
    // is already wired; pass your own only to override it.
    livePreviewUrl: ({ data, req }) => generateLivePreviewPath({ data, req }),
    extraSettingsFields: [],
    fields: [],
    iconRowFields: [],
    useAsTitle: 'title',
    group: 'Sets',
    // hooks is unset
  },
})
```

## Caching & revalidation

The reads this package ships (`@pro-laico/icons/cache`) are server-side and cached, so a page resolves an icon without re-querying Payload on every render:

- `getCachedIconSet(draft)`: the active icon set's `name` + reference list.
- `getCachedIconByName(name, draft, iconSet)`: the SVG string for one name, resolved through that set.
- `getCachedIconOptions(draft, iconSet)`: the set as `{ label, value }` options for the admin icon picker.

When you save or delete an `Icon` or `IconSet`, the collections' `@pro-laico/core` revalidation hooks bust the matching tags (`iconSet`, and each icon's own `icon` tag). Editing the active set re-routes lookups; editing one icon invalidates only that icon, so unrelated `<svg>` output stays cached. See [Caching & revalidation](/docs/concepts/caching) for how the tags and `withCache` work.

## Exports

Grouped by what each export is for.

### The plugin

<ExportTable
  exports={{
    iconsPlugin: {
      type: 'plugin',
      location: '@pro-laico/icons',
      description: 'The plugin itself. Registers the Icon collection and, by default, the IconSet collection. Default and named export.',
      params: {
        'options?': { type: 'IconsPluginOptions', description: 'See the Options table above. Every key is optional; IconSet live preview is wired by default, so pass `iconSetOptions.livePreviewUrl` only to override it, or `includeIconSet: false` to register the upload collection only.' },
      },
      returns: { type: 'Plugin', description: 'A Payload config plugin you add to `buildConfig({ plugins: [...] })`.' },
      example: `import { buildConfig } from 'payload'
import { iconsPlugin } from '@pro-laico/icons'
import { generateLivePreviewPath } from '@pro-laico/core'

export default buildConfig({
  plugins: [
    // IconSet live preview is on by default; this shows overriding the URL builder.
    iconsPlugin({
      iconSetOptions: {
        livePreviewUrl: ({ data, req }) => generateLivePreviewPath({ data, req }),
      },
    }),
  ],
})`,
    },
    IconsPluginOptions: { type: 'type', location: '@pro-laico/icons', description: "The TypeScript type for the plugin's options." },
  }}
/>

### Frontend components

<ExportTable
  exports={{
    Icon: {
      type: 'component',
      location: '@pro-laico/icons/Icon',
      description: 'Server component that renders an icon by name from the active set, with cached lookup and a fallback. The main way to put a CMS-managed icon on the frontend. Default and named export.',
      params: {
        name: { type: 'string', description: 'The icon name to look up in the active IconSet (each entry’s kebab-cased `name`).' },
        'fallback?': { type: 'string', description: 'Raw SVG string rendered when `name` matches nothing in the active set. Defaults to a small inline warning glyph.' },
        '...svgProps?': { type: 'React.SVGAttributes<SVGSVGElement>', description: 'Any `<svg>` props (`className`, `style`, `width`, …). They ALWAYS win over the source SVG’s intrinsic attributes.' },
      },
      returns: { type: 'Promise<JSX.Element>', description: 'An inlined `<svg>` (async server component).' },
      exampleLang: 'tsx',
      example: `import { Icon } from '@pro-laico/icons/Icon'

// A server component that renders icons from the active set by name:
export function Toolbar() {
  return (
    <nav>
      <Icon name="arrow-right" />
      <Icon name="arrow-right" className="size-6 text-primary" />
      <Icon name="logo" fallback={myCustomSvgString} />
    </nav>
  )
}`,
    },
    AtomicIcon: {
      type: 'component',
      location: '@pro-laico/icons/AtomicIcon',
      description: 'A small, self-contained branding glyph (the Atomic Payload mark), tinted by node type. Not a CMS lookup: it takes no `name` and never reads an IconSet. Use it for admin/editor chrome that labels atomic node kinds, not for content icons. Default and named export.',
      params: {
        type: { type: "'tag' | 'form' | 'input' | 'button' | 'portal'", description: 'Picks the fill/stroke color from the atomic distinct-color CSS variables (one per node kind).' },
      },
      returns: { type: 'JSX.Element', description: 'A fixed 24x24 `<svg>` glyph tinted for that node type.' },
      exampleLang: 'tsx',
      example: `import { AtomicIcon } from '@pro-laico/icons/AtomicIcon'

// Label an atomic node kind in editor chrome:
export function NodeBadge() {
  return (
    <span className="flex items-center gap-2">
      <AtomicIcon type="button" />
      Button
    </span>
  )
}`,
    },
  }}
/>

### SVG helpers

<ExportTable
  exports={{
    extractSvgContent: {
      type: 'function',
      location: '@pro-laico/icons',
      description: 'Pulls the inner markup out of an SVG string (everything between the `<svg>` tags), so you can re-inline it on a fresh `<svg>` node. Returns the original string if it has no `<svg>` wrapper.',
      params: {
        svgString: { type: 'string', description: 'A full SVG document string.' },
      },
      returns: { type: 'string', description: 'The inner SVG markup (paths, groups, …) without the wrapper tags.' },
      exampleLang: 'tsx',
      example: `import { extractSvgContent, extractSvgProps } from '@pro-laico/icons'

// Render a stored SVG string as a native <svg>, the way IconChild does:
function SvgFromString({ svg }: { svg: string }) {
  return <svg {...extractSvgProps(svg)} dangerouslySetInnerHTML={{ __html: extractSvgContent(svg) }} />
}`,
    },
    extractSvgProps: {
      type: 'function',
      location: '@pro-laico/icons',
      description: 'Pulls the attributes (`viewBox`, `fill`, `xmlns`, …) off an SVG’s root `<svg>` tag as a plain props object you can spread onto a JSX `<svg>`. Handles namespaced and hyphenated attribute names.',
      params: {
        svgString: { type: 'string', description: 'A full SVG document string.' },
      },
      returns: { type: 'Record<string, string>', description: 'The root tag’s attributes as `{ name: value }`. Empty object when there is no `<svg>` tag.' },
      exampleLang: 'tsx',
      example: `import { extractSvgContent, extractSvgProps } from '@pro-laico/icons'

const source = icon.svgString
// JSX props you add after the spread win over the source's intrinsic attrs:
return <svg {...extractSvgProps(source)} className="size-6" dangerouslySetInnerHTML={{ __html: extractSvgContent(source) }} />`,
    },
  }}
/>

### Content blocks

<ExportTable
  exports={{
    createIconBlock: {
      type: 'function',
      location: '@pro-laico/icons/blocks/iconChild',
      description: 'Factory that builds the `IconChild` content block (an editor picks an icon by name from the active set). `prependFields` / `appendFields` are spread at the start / end of the Icon tab, so the consumer decides what goes there and the block carries no CSS dependency of its own.',
      params: {
        'options?': { type: 'IconBlockOptions', description: '`{ prependFields?, appendFields? }`: extra fields to spread before / after the icon picker on the Icon tab.' },
      },
      returns: { type: 'Block', description: 'A Payload `Block` you add to a `blocks` field (e.g. the atomic children blocks list).' },
      example: `import { createIconBlock, Icon as IconChildBlock } from '@pro-laico/icons/blocks/iconChild'
import { ClassNameField } from '@pro-laico/styles/fields/className'

// Default block, or build one that carries a className field:
const StyledIconBlock = createIconBlock({ prependFields: [ClassNameField({ namePrefix: 'icon' })] })

// Expose it on whatever blocks field hosts children:
{ type: 'blocks', name: 'children', blocks: [IconChildBlock, StyledIconBlock] }`,
    },
    Icon: { type: 'object', location: '@pro-laico/icons/blocks/iconChild', description: 'The default `IconChild` block, equivalent to `createIconBlock()` with no extra fields. Import this when you do not need to extend it.' },
    IconBlockOptions: { type: 'type', location: '@pro-laico/icons/blocks/iconChild', description: 'The options type for `createIconBlock` (`prependFields` / `appendFields`).' },
    IconChild: {
      type: 'component',
      location: '@pro-laico/icons/blocks/iconChild/component',
      description: 'Server component that renders a saved `IconChild` block: resolves the stored icon name through the active set with the same cached lookup + warning fallback as `<Icon>`. Rendered for you by `@pro-laico/atomic`’s children renderer.',
      params: {
        block: { type: 'RenderChild<IconChild>', description: 'The block data and pass-through props supplied by the atomic children renderer.' },
      },
      returns: { type: 'Promise<JSX.Element>', description: 'The resolved icon as an inlined `<svg>` (async server component).' },
      exampleLang: 'tsx',
      example: `import { IconChild } from '@pro-laico/icons/blocks/iconChild/component'

// The atomic children renderer maps the IconChild block slug to this component.
// You rarely render it by hand, but it is an async server component:
<IconChild block={childData} pt={passThroughProps} />`,
    },
    createSvgBlock: {
      type: 'function',
      location: '@pro-laico/icons/blocks/svgChild',
      description: 'Factory that builds the `SVGChild` content block (an editor pastes raw SVG: viewBox, fill, and the path data). `prependFields` / `appendFields` are spread at the start / end of the Content tab, so the block carries no CSS dependency of its own.',
      params: {
        'options?': { type: 'SvgBlockOptions', description: '`{ prependFields?, appendFields? }`: extra fields to spread before / after the SVG inputs on the Content tab.' },
      },
      returns: { type: 'Block', description: 'A Payload `Block` you add to a `blocks` field.' },
      example: `import { createSvgBlock, SVGBlock } from '@pro-laico/icons/blocks/svgChild'

// Default block, or one with extra fields:
const block = createSvgBlock()

// Expose it on a children blocks field next to IconChild:
{ type: 'blocks', name: 'children', blocks: [SVGBlock] }`,
    },
    SVGBlock: { type: 'object', location: '@pro-laico/icons/blocks/svgChild', description: 'The default `SVGChild` block, equivalent to `createSvgBlock()` with no extra fields.' },
    SvgBlockOptions: { type: 'type', location: '@pro-laico/icons/blocks/svgChild', description: 'The options type for `createSvgBlock` (`prependFields` / `appendFields`).' },
    SVGChild: {
      type: 'component',
      location: '@pro-laico/icons/blocks/svgChild/component',
      description: 'Server component that renders a saved `SVGChild` block: inlines the pasted SVG attributes and contents onto a native `<svg>`. Rendered for you by `@pro-laico/atomic`’s children renderer.',
      params: {
        block: { type: 'RenderChild<SVGChild>', description: 'The block data and pass-through props supplied by the atomic children renderer.' },
      },
      returns: { type: 'JSX.Element', description: 'The pasted SVG as an inlined `<svg>`.' },
      exampleLang: 'tsx',
      example: `import { SVGChild } from '@pro-laico/icons/blocks/svgChild/component'

// Mapped to the SVGChild block slug by the atomic children renderer:
<SVGChild block={childData} pt={passThroughProps} />`,
    },
  }}
/>

### Cache getters

<ExportTable
  exports={{
    getCachedIconSet: {
      type: 'function',
      location: '@pro-laico/icons/cache',
      description: 'Cached read of the active icon set’s entries (each `name` plus its icon reference id). Wrapped so a page reads it once per request and revalidated when an IconSet is saved or deleted. Resolve it first, then pass it to the two getters below.',
      params: {
        draft: { type: 'boolean', description: 'Read the draft (`true`) or published (`false`) variant.' },
      },
      returns: { type: 'Promise<IconSetReturn>', description: '`{ iconsArray: { name, icon }[] }` for the active set.' },
      exampleLang: 'tsx',
      example: `import { draftMode } from 'next/headers'
import { getCachedIconByName, getCachedIconSet } from '@pro-laico/icons/cache'

// Resolve the active set once, then look icons up by name against it:
const { isEnabled: draft } = await draftMode()
const iconSet = await getCachedIconSet(draft)
const svg = await getCachedIconByName('check', draft, iconSet)`,
    },
    getCachedIconByName: {
      type: 'function',
      location: '@pro-laico/icons/cache',
      description: 'Cached read of one icon’s SVG string, resolved by name through the active set. This is what `<Icon>` calls internally. Each icon sits behind its own `icon` tag, so editing one icon invalidates only that icon, not the whole page.',
      params: {
        name: { type: 'string', description: 'The icon name to resolve in the active set.' },
        draft: { type: 'boolean', description: 'Draft or published variant.' },
        iconSet: { type: 'IconSetReturn | undefined', description: 'The active set from `getCachedIconSet(draft)`.' },
      },
      returns: { type: 'Promise<string | undefined>', description: 'The icon’s SVG string, or `undefined` when the name matches nothing.' },
      exampleLang: 'tsx',
      example: `import { draftMode } from 'next/headers'
import { extractSvgContent, extractSvgProps } from '@pro-laico/icons'
import { getCachedIconByName, getCachedIconSet } from '@pro-laico/icons/cache'

// A hand-rolled icon-by-name server component (what <Icon> wraps):
async function IconByName({ name }: { name: string }) {
  const { isEnabled: draft } = await draftMode()
  const iconSet = await getCachedIconSet(draft)
  const svg = await getCachedIconByName(name, draft, iconSet)
  if (!svg) return null
  return <svg {...extractSvgProps(svg)} dangerouslySetInnerHTML={{ __html: extractSvgContent(svg) }} />
}`,
    },
    getCachedIconOptions: {
      type: 'function',
      location: '@pro-laico/icons/cache',
      description: 'Cached read of the active set as `{ label, value }` options for an admin select. The bundled `IconSelect` field uses it so the picker stays in sync with the active set.',
      params: {
        draft: { type: 'boolean', description: 'Draft or published variant.' },
        iconSet: { type: 'IconSetReturn | undefined', description: 'The active set from `getCachedIconSet(draft)`.' },
      },
      returns: { type: 'Promise<{ label: string; value: string }[]>', description: 'One option per icon in the active set, value = name.' },
      exampleLang: 'tsx',
      example: `import { getCachedIconOptions, getCachedIconSet } from '@pro-laico/icons/cache'

// In an admin server field, build the icon picker's options from the active set:
const iconSet = await getCachedIconSet(true)
const options = await getCachedIconOptions(true, iconSet)`,
    },
  }}
/>

### Admin

<ExportTable
  exports={{
    IconSelect: {
      type: 'component',
      location: '@pro-laico/icons/admin/iconSelect',
      description: 'Admin server field that renders a select whose options are the active icon set’s icons (via `getCachedIconOptions`). Wire it as a field’s `Field` component to let editors pick an icon by name. Default and named export.',
      params: {
        '...props': { type: 'SelectFieldServerComponent props', description: 'The `clientField`, `path`, `schemaPath`, and `permissions` Payload passes to a server field component.' },
      },
      returns: { type: 'Promise<JSX.Element>', description: 'A Payload `SelectField` populated with the active set’s icon names.' },
      example: `// In a field config, render the icon picker as that field's admin component:
{
  name: 'icon',
  type: 'text',
  admin: { components: { Field: { path: '@pro-laico/icons/admin/iconSelect' } } },
}`,
    },
    IconLabelPath: { type: 'constant', location: '@pro-laico/icons', description: 'The admin import-map path for the IconSet row label component. Re-export it from your `ui` barrel so the generated import map can resolve it.' },
  }}
/>

### Usage scan & panel

The build-time scanner, the manifest readers, and the admin panel that powers the [Requested-icons usage panel](#requested-icons-usage-panel). The scanner is also published as the package's `atomic-icons-scan` command, so you can run it without importing anything.

<ExportTable
  exports={{
    scanIconUsages: {
      type: 'function',
      location: '@pro-laico/icons/scan',
      description: 'Walks a project’s source tree and builds the usage manifest by extracting every literal `<Icon name="…">`. Writes nothing itself: pair it with `writeIconUsageManifest` to persist the result.',
      params: {
        'options?': { type: 'ScanOptions', description: '`{ roots?, cwd?, components?, extensions?, ignore? }`: which directories to walk (default `['src', 'app']`), which tag names count as icons (default `['Icon']`), and which file extensions/dirs to include or skip.' },
      },
      returns: { type: 'ScanResult', description: '`{ manifest, filesScanned }`: the assembled manifest plus the number of files read.' },
      example: `import { scanIconUsages, writeIconUsageManifest, resolveManifestPath } from '@pro-laico/icons/scan'

// In a build script (e.g. behind a prebuild step):
const { manifest } = scanIconUsages({ roots: ['src', 'app'] })
writeIconUsageManifest(manifest, resolveManifestPath())`,
    },
    writeIconUsageManifest: {
      type: 'function',
      location: '@pro-laico/icons/scan',
      description: 'Writes a manifest to disk as pretty-printed JSON (with a trailing newline). Returns the absolute path written.',
      params: {
        manifest: { type: 'IconUsageManifest', description: 'The manifest produced by `scanIconUsages`.' },
        path: { type: 'string', description: 'Output path; resolved against the working directory when relative.' },
      },
      returns: { type: 'string', description: 'The absolute path the file was written to.' },
      example: `import { scanIconUsages, writeIconUsageManifest, resolveManifestPath } from '@pro-laico/icons/scan'

const { manifest } = scanIconUsages()
const path = writeIconUsageManifest(manifest, resolveManifestPath())`,
    },
    loadIconUsageManifest: {
      type: 'function',
      location: '@pro-laico/icons/scan',
      description: 'Reads and parses a manifest from disk. Returns `null` when the file is absent or unparseable, so callers can render a "run the scan" empty state rather than throwing. This is what the admin panel uses to load the manifest server-side.',
      params: {
        'path?': { type: 'string', description: 'Explicit manifest path; resolved via `resolveManifestPath` when omitted.' },
        'cwd?': { type: 'string', description: 'Working directory the default path resolves against (defaults to `process.cwd()`).' },
      },
      returns: { type: 'IconUsageManifest | null', description: 'The parsed manifest, or `null` when missing/invalid.' },
      example: `import { loadIconUsageManifest } from '@pro-laico/icons/scan'

// Server-side: null means no manifest yet, so prompt the editor to run the scan.
const manifest = loadIconUsageManifest()`,
    },
    resolveManifestPath: {
      type: 'function',
      location: '@pro-laico/icons/scan',
      description: 'Resolves the manifest path in order: an explicit argument, then the `ICON_USAGE_MANIFEST` env var, then the default `icon-usage-manifest.json` filename under the working directory. The scanner, the CLI, and the panel all resolve the path the same way.',
      params: {
        'explicit?': { type: 'string', description: 'A path that wins over the env var and default when provided.' },
        'cwd?': { type: 'string', description: 'Working directory the default filename resolves against (defaults to `process.cwd()`).' },
      },
      returns: { type: 'string', description: 'The absolute manifest path.' },
      example: `import { scanIconUsages, writeIconUsageManifest, resolveManifestPath } from '@pro-laico/icons/scan'

const { manifest } = scanIconUsages()
writeIconUsageManifest(manifest, resolveManifestPath())`,
    },
    extractIconUsages: {
      type: 'function',
      location: '@pro-laico/icons/scan',
      description: 'The pure, `fs`-free parser at the core of the scan: pulls every literal `name` out of one file’s text. Tag-aware (it parses each `<Icon …>` attribute list rather than running a naive regex) and collects only static string literals. Use it to scan source you already have in memory.',
      params: {
        source: { type: 'string', description: 'The full text of a single source file.' },
        'options?': { type: 'ExtractOptions', description: '`{ components? }`: tag names to treat as icon usages (default `['Icon']`).' },
      },
      returns: { type: 'ExtractedUsage[]', description: 'Each literal usage as `{ name, line, column }`, in source order.' },
      example: `import { extractIconUsages } from '@pro-laico/icons/scan'

// Names found in one file's text, without touching the filesystem:
const usages = extractIconUsages(fileSource)`,
    },
    IconUsagePanel: {
      type: 'component',
      location: '@pro-laico/icons/admin/iconUsagePanel',
      description: 'The server `UIField` that renders the "Requested icons" panel. Reads the build-time manifest (and the runtime `iconRequest` misses when tracking is on) and diffs them against the set you’re editing. Wired for you by `usagePanel: true`; you only import its path constant. Default and named export.',
      params: {
        '...props': { type: 'UIFieldServerComponent props (manifestPath?, scanCommand?)', description: 'The `serverProps` Payload passes through, set by `createIconSetCollection` from `usageManifestPath` / `usageScanCommand`.' },
      },
      returns: { type: 'Promise<JSX.Element>', description: 'The rendered Requested-icons panel (async server component).' },
      example: `// You don't render this directly. Turning on usagePanel wires it:
iconsPlugin({ iconSetOptions: { usagePanel: true } })`,
    },
    IconUsagePanelPath: { type: 'constant', location: '@pro-laico/icons', description: 'The admin import-map path for the usage panel. Re-export it from your `ui` barrel (the same way you re-export `IconLabelPath`) so `generate:importmap` can resolve it.' },
  }}
/>

### Types

<ExportTable
  exports={{
    'Icon (type)': { type: 'type', location: '@pro-laico/icons/schema', description: 'TypeScript type for typed access to your Icon documents.' },
    'IconSet (type)': { type: 'type', location: '@pro-laico/icons/schema', description: 'TypeScript type for typed access to your IconSet documents.' },
    IconSetReturn: { type: 'type', location: '@pro-laico/icons/cache', description: 'The shape `getCachedIconSet` returns: `{ iconsArray: { name, icon }[] }`.' },
    IconUsageManifest: { type: 'type', location: '@pro-laico/icons/scan', description: 'The shape of the usage manifest the scan writes and the panel reads: `{ version, generatedAt, names, usages }`.' },
    IconUsage: { type: 'type', location: '@pro-laico/icons/scan', description: 'One recorded usage in the manifest: `{ name, file, line, column }`.' },
  }}
/>

## Related

<Cards>
  <Card title="Icons" href="/docs/features/icons" />
  <Card title="@pro-laico/styles" href="/docs/plugins/styles" />
  <Card title="@pro-laico/atomic" href="/docs/plugins/atomic" />
</Cards>
